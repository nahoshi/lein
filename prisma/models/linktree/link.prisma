model LinkTreeItem {
    id    Int    @id @default(autoincrement())
    title String
    url   String

    iconId Int?          @unique
    icon   LinkTreeIcon? @relation(fields: [iconId], references: [id])

    sessionId Int
    session   LinkTreeSession @relation(fields: [sessionId], references: [id])
}
